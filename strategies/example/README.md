# Example Template Strategy

A minimal crossover example demonstrating the repository layout. Replace this with your own idea and use `pnpm lint:pine` to catch issues before pasting into TradingView.
